# Dev / test dependencies — mirrors pyproject.toml [tool.uv.dev-dependencies].
# Generated with: uv pip compile pyproject.toml --extra dev -o requirements-dev.txt
pytest>=8.3
pytest-cov>=5.0
mypy>=1.10
ruff>=0.4
pre-commit>=3.7
